// import ScrollTarget from './ScrollTarget'

// //query selector for the divs clicked
// const service = document.querySelector('.LogoServices')
// const gallery = document.querySelector('.gallery')
// const home = document.querySelector('.home')
// const serviceBottom = document.querySelector('.serviceBottom')
// const galleryBottom = document.querySelector('.gallerybottom')

// //viewport width and height for the document viewport
// const vw = Math.max(document.documentElement.clientWidth || 0, window.innerWidth || 0)
// const vh = Math.max(document.documentElement.clientHeight || 0, window.innerHeight || 0)

// //new scrolltargets referencing the scrolls
// const services = new ScrollTarget(0, vh - 100)
// const galleries = new ScrollTarget(0, vh * 3.5)
// const GoHome = new ScrollTarget(0, 0)

// service.addEventListener('click', () => services.scrollTo() )
// gallery.addEventListener('click', () => galleries.scrollTo())
// home.addEventListener('click', () => GoHome.scrollTo())